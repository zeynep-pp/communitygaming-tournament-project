

extend type Query {
    tournaments(userId: ID!): [Tournament]
}

type Tournament {
    userId:  ID!
    tournamentName: String!
    perTeamNumber:String!
}


extend type Mutation {
    newTournament(userId: String!, tournamentName: String!, perTeamNumber:String!) : Tournament!
}