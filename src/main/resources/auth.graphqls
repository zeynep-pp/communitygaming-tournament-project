
type JWTToken {
    idToken: String!
}

input LoginInput {
    username: String!
    password: String!
    isRememberMe: Boolean!
}

extend type Mutation {
    login(loginInput: LoginInput): JWTToken
}